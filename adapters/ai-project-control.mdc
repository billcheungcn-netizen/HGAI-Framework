---
description: AI 大型软件开发控制法入口
alwaysApply: true
---

开始任何任务前，先阅读项目根目录的 `AGENTS.md`、`docs/rules/INDEX.md`、`docs/coordination/ACTIVE_TASKS.md` 和当前任务适用规则。生成唯一任务 ID，检查目标模块和文件是否已被其他任务认领。

必须先用终端用户语言确认需求，再定位模块并建立任务文档。技术过程写入内部文档；面向项目负责人的反馈必须说明功能、需求、使用方法、页面或行为变化和用户收益，不得默认对方是专业程序员。

按 Token 定位阶梯从指定文件或模块开始，只有证据表明范围不足时才扩大搜索。

不得绕过 Git 基线、统一本地验证、远端 CI（适用时）、自动化测试、文档防腐、架构边界、依赖和密钥治理规则。作为审核 AI 时默认只读，不直接修改作者工作区。
