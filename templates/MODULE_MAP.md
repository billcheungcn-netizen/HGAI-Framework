---
id: SYSTEM-MODULE-MAP
status: active
created: YYYY-MM-DD
updated: YYYY-MM-DD
source_of_truth: true
supersedes:
superseded_by:
related_commits: []
---

# 模块地图

> 目标是让人和 AI 在不扫描全仓的情况下，从需求快速定位到责任模块、目录和入口文件。

## 1. 顶层目录

| 目录 | 内容 | 何时进入 | 不应放入 |
|---|---|---|---|
| `<路径>` | `<职责>` | `<任务类型>` | `<禁止内容>` |

## 2. 模块清单

| 模块 | 目录 | 终端用户功能 | 技术职责 | 主要入口 | 允许依赖 | 禁止依赖 | 详细文档 |
|---|---|---|---|---|---|---|---|
| `<模块>` | `<路径>` | `<用户得到什么>` | `<职责>` | `<文件或符号>` | `<模块>` | `<模块>` | `<路径>` |

## 3. 按需求类型定位

| 如果需求涉及 | 首先查看模块 | 首先查看文件 | 相关文档 | 不要先改 |
|---|---|---|---|---|
| `<需求类型>` | `<模块>` | `<路径>` | `<路径>` | `<错误但容易被选中的位置>` |

## 4. 按问题现象定位

| 问题现象 | 可能责任模块 | 建议调查入口 | 常见误判 |
|---|---|---|---|
| `<现象>` | `<模块>` | `<路径或日志>` | `<说明>` |

## 5. 跨模块调用

| 调用方 | 被调用方 | 唯一允许入口 | 数据方向 | 禁止方式 |
|---|---|---|---|---|
| `<模块>` | `<模块>` | `<接口>` | `<方向>` | `<说明>` |

## 6. 地图更新规则

发生以下情况必须更新本文件：

- 新增、删除或移动模块；
- 模块职责发生变化；
- 主要入口发生变化；
- 依赖方向发生变化；
- AI 曾因地图不清而找错模块；
- 某类需求需要读取大量无关文件才能定位。
