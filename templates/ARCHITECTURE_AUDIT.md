---
id: AUDIT-YYYYMMDD-HHMMSS-AGENT-XXXX
status: active
created: YYYY-MM-DD
updated: YYYY-MM-DD
source_of_truth: true
claimed_by:
reviewed_by: []
target_modules: []
target_files: []
claimed_at:
last_activity_at:
branch:
related_commits: []
---

# 架构审计：`<范围或日期>`

## 1. 审计范围

- 模块：`<填写>`
- 目录：`<填写>`
- 触发原因：`定期检查 / AI 多次跑偏 / Token 上升 / 需求难以定位 / 重复 Bug`
- 相关任务：`<路径>`
- **[AI填写][人审-用户] 受影响的终端用户功能：** `<功能和当前影响>`
- **[工具证明] Git 基线、任务分支和当前认领：** `<填写>`

## 2. 当前模块职责

| 模块 | 应有职责 | 实际职责 | 是否一致 |
|---|---|---|---|
| `<模块>` | `<填写>` | `<填写>` | `<是/否>` |

## 3. 依赖检查

| 来源模块 | 目标模块 | 依赖是否允许 | 问题 |
|---|---|---|---|
| `<来源>` | `<目标>` | `<是/否>` | `<说明>` |

检查：

- [ ] 依赖保持单向
- [ ] 没有循环依赖
- [ ] 展示层没有复制业务逻辑
- [ ] 基础层没有反向依赖上层业务
- [ ] 模块之间通过明确接口协作

## 4. AI 漂移检查

- [ ] 同一职责只有一个权威实现
- [ ] 没有新增平行实现
- [ ] 没有用兜底隐藏根因
- [ ] 没有无期限保留兼容分支
- [ ] 没有跨模块修改无关业务
- [ ] 文件和目录名称能够表达职责
- [ ] AI 能通过索引快速找到正确模块
- [ ] 文档描述与代码现实一致

## 5. Token 与认知路径

- 完成一个典型需求需要读取哪些文档：`<填写>`
- 需要读取哪些目录和文件：`<填写>`
- 是否需要扫描大量无关代码：`<填写>`
- 可以补充或修正什么索引：`<填写>`
- 可以通过哪些边界调整缩短定位路径：`<填写>`

## 6. 发现的问题

| 优先级 | 问题 | 根因 | 影响 | 建议目标结构 |
|---|---|---|---|---|
| `<高/中/低>` | `<问题>` | `<根因>` | `<影响>` | `<目标>` |

## 7. 重构计划

重构必须减少认知路径和结构复杂度，不能只增加一层包装或兼容。

1. `<第一步，必须可独立验证>`
2. `<第二步>`
3. `<第三步>`

## 8. 完成标准

- [ ] 模块职责恢复清晰
- [ ] 依赖方向符合架构
- [ ] 旧实现和临时兼容已删除
- [ ] 相关测试全部通过
- [ ] 模块地图和索引已更新
- [ ] 下一个相似任务能够更快定位
- [ ] 终端用户功能没有意外变化，或计划中的变化已经用用户语言说明并验收
- [ ] 每个重构检查点都有自动化测试和关联 Commit
- [ ] 远端 CI 在适用时已通过，且审核 AI 已对目标 Commit 完成只读复核
