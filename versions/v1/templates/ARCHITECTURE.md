# 项目架构

## 1. 架构目标

`<说明为什么选择当前架构，以及它要保护什么>`

## 2. 分层与依赖方向

```text
<在此画出本项目真实的单向依赖关系>
```

硬性规则：

- `<上层可以依赖什么>`
- `<下层禁止反向依赖什么>`
- `<跨模块只能通过什么接口>`
- `<展示层和业务层的边界>`

## 3. 模块边界

| 模块 | 唯一职责 | 拥有的数据或规则 | 对外接口 | 禁止承担 |
|---|---|---|---|---|
| `<模块>` | `<职责>` | `<内容>` | `<接口>` | `<禁止内容>` |

## 4. 权威实现

| 职责 | 唯一权威位置 | 其他模块如何使用 | 禁止的平行实现 |
|---|---|---|---|
| `<职责>` | `<路径>` | `<接口>` | `<说明>` |

## 5. 数据流和控制流

### `<主要流程>`

```text
输入 → 接口 → 应用逻辑 → 领域逻辑 → 基础设施 → 输出
```

涉及文件：`<填写>`

## 6. 负面架构约束

- 禁止用默认值或吞错代替根因修复。
- 禁止长期保留新旧两套实现。
- 禁止展示层复制领域规则。
- 禁止跨模块直接访问内部实现。
- 禁止为单个需求破坏整体依赖方向。
- 项目特有禁止项：`<填写>`

## 7. 验证架构的方法

- 自动化检查：`<命令或工具>`
- 人工检查：`<步骤>`
- 关键测试：`<路径>`

## 8. 已知架构问题

| 问题 | 影响 | 临时状态 | 计划文档 |
|---|---|---|---|
| `<问题>` | `<影响>` | `<状态>` | `<路径>` |

