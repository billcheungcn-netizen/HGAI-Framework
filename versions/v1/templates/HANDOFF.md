# 工作交接与上下文恢复：`<任务>`

## 1. 当前目标

`<用一段话说明现在究竟要完成什么>`

## 2. 已完成

- `<已完成事项和对应文件>`

## 3. 当前真实状态

- 当前分支或版本：`<填写>`
- 当前修改文件：`<路径>`
- 当前能否构建：`<是/否及结果>`
- 当前能否测试：`<是/否及结果>`
- 已知未解决问题：`<填写>`

## 4. 下一步

1. `<精确的下一步>`
2. `<后续步骤>`

## 5. 恢复工作前必须阅读

1. `AGENTS.md`
2. `<当前任务文档>`
3. `<相关架构或模块文档>`
4. `<关键代码文件>`

## 6. 禁止事项

- `<不能改的模块>`
- `<不能采用的临时方案>`
- `<已经证明错误的方向>`

## 7. 最近一次验证

| 项目 | 方法 | 结果 |
|---|---|---|
| `<验证>` | `<命令或步骤>` | `<结果>` |

## 8. 恢复后的第一条指令

```text
先完整阅读 AGENTS.md、本交接文档和当前任务文档，复述项目现状、下一步、目标模块与禁止事项。未经重新确认，不要直接修改代码。
```

