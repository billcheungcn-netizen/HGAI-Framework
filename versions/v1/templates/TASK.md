# 需求任务：`<名称>`

## 1. 元信息

- 状态：`待定位 / 待审核 / 执行中 / 待验证 / 已完成 / 已暂停`
- 创建时间：`<时间>`
- 责任模块：`<模块>`
- 关联需求：`<来源>`
- 关联文档：`<路径列表>`

## 2. 原始需求

`<保留用户原话，不要只写 AI 的转述>`

## 3. AI 对需求的理解

- 要解决的问题：`<填写>`
- 用户最终应看到的结果：`<填写>`
- 明确不做的事情：`<填写>`
- 仍有歧义的内容：`<填写>`

## 4. 现状和证据

- 当前实现入口：`<文件和符号>`
- 当前行为：`<填写>`
- 已阅读文档：`<路径>`
- 已阅读代码：`<路径>`
- 为什么责任属于该模块：`<说明>`

## 5. 目标范围

### 允许修改

- `<文件或目录>`：`<原因>`

### 禁止修改

- `<文件、目录或模块>`：`<原因>`

## 6. 架构约束

- 必须保持的依赖方向：`<填写>`
- 必须复用的权威实现：`<填写>`
- 禁止出现的兜底或兼容：`<填写>`
- 不得侵入的业务：`<填写>`
- 其他负面约束：`<填写>`

## 7. 执行计划

1. `<具体到文件、模块或接口的步骤>`
2. `<步骤>`
3. `<步骤>`

如果计划只能写成“搜索相关代码并修改”，返回定位阶段继续调查。

## 8. 验收标准

- [ ] `<可观察、可验证的结果>`
- [ ] `<相关旧功能仍然正常>`
- [ ] `<构建、测试或类型检查要求>`
- [ ] `<没有新增跨层依赖、重复实现或未经批准的兜底>`

## 9. 人工计划检查

- [ ] 需求理解正确
- [ ] 责任模块正确
- [ ] 目标文件正确
- [ ] 未扩大任务范围
- [ ] 架构约束完整
- [ ] 验证方法足够

- 审核结论：`批准 / 修改后批准 / 暂停`
- 审核备注：`<填写>`

## 10. 实际执行记录

| 修改 | 文件 | 原因 | 与计划的差异 |
|---|---|---|---|
| `<内容>` | `<路径>` | `<原因>` | `<无或说明>` |

## 11. 验证结果

| 验证项 | 执行方式 | 实际结果 |
|---|---|---|
| `<测试>` | `<命令或步骤>` | `<通过/失败及证据>` |

## 12. 最终状态

- 完成内容：`<填写>`
- 未完成内容：`<填写>`
- 遗留风险：`<填写>`
- 新增文档：`<路径>`
- 已更新索引：`是 / 否`
- 是否需要架构审计或重构：`是 / 否，原因`

