---
id: BUG-YYYYMMDD-NNN
status: draft
created: YYYY-MM-DD
updated: YYYY-MM-DD
source_of_truth: true
supersedes:
superseded_by:
related_commits: []
---

# Bug：`<终端用户能够理解的问题名称>`

开始前必须阅读 `docs/rules/bug-diagnosis.md`、`docs/rules/testing.md` 和当前模块规则。

## 1. 终端用户现象

- **[AI填写] 项目负责人原话：** `<保留原始描述>`
- **[AI填写][人审] 用户在做什么：** `<入口和操作>`
- **[AI填写][人审] 用户看到了什么问题：** `<页面、提示、状态或结果>`
- **[AI填写][人审] 正常情况下应该怎样：** `<用户期望>`
- **[AI填写][人审] 对用户造成什么影响：** `<无法完成什么、多做什么或承担什么风险>`
- **[AI填写] 相关标准术语：** `<GLOSSARY.md>`

## 2. 复现和证据

- **[AI填写] 复现条件：** `<环境、数据、账户状态等>`
- **[AI填写] 复现步骤：** `<可重复步骤>`
- **[工具生成] 实际结果：** `<真实观察>`
- **[工具生成] 日志、截图或错误位置：** `<不得包含密钥>`
- **[AI填写] 相关历史 Bug 和规则：** `<路径>`

## 3. 修复前失败测试

- **[AI填写] 自动化测试位置：** `<路径>`
- **[工具生成] 修复前结果：** `<失败输出>`
- **[AI填写] 测试覆盖的用户行为：** `<说明>`
- **[AI填写][人审] 无法自动化的例外：** `<无；如有填写原因、人工证据和后续任务>`

没有测试体系时，必须记录缺口并建立测试基础设施任务。

## 4. 根因定位

- **[AI填写] 责任模块和目标文件：** `<填写>`
- **[AI填写] 调用链：** `<填写>`
- **[AI填写] 根因证据：** `<不能只写猜测>`
- **[AI填写] 为什么不是其他模块：** `<填写>`
- **[AI填写] 是否存在旧兜底、兼容或重复逻辑：** `<填写>`

证据不足时状态保持“待定位”，不得把推测写成根因。

## 5. 修复约束

- **[AI填写][人审] 禁止范围：** `<模块和文件>`
- 禁止吞掉错误或静默失败；
- 禁止用默认值、无证据重试或新兜底覆盖根因；
- 禁止复制另一套实现；
- 禁止修改无关模块；
- 修复测试和回归测试必须同批入库；
- 新依赖、权限和密钥风险必须先报告。

## 6. Git 基线

- **[工具生成] 分支、HEAD 和工作区状态：** `<填写>`
- **[AI填写] 用户未提交修改保护：** `<填写>`
- **[人审] Git 权限：** `<填写>`
- **[AI填写] 检查点和回退计划：** `<填写>`

## 7. 修复计划

1. **[AI填写]** `<文件级步骤>`
2. **[AI填写]** `<步骤>`
3. **[AI填写]** `<步骤>`

## 8. 人工方向检查

- [ ] 用户现象和影响描述正确
- [ ] 复现方法可靠
- [ ] 修复前失败测试已落地或例外已说明
- [ ] 根因有证据支持
- [ ] 责任模块正确
- [ ] 不会引入兜底、兼容或重复实现
- [ ] 修复后用户行为和验收方法明确

- **[人审] 结论：** `批准 / 修改后批准 / 暂停`

## 9. 实际修改与验证

| [自动更新] 内容 | 文件或步骤 | 实际结果 | 证据 |
|---|---|---|---|
| `<内容>` | `<路径/命令>` | `<结果>` | `<位置>` |

- **[工具生成] 修复前复现测试：** `<失败>`
- **[工具生成] 修复后复现测试：** `<通过>`
- **[工具生成] 相关回归：** `<结果>`
- **[工具生成] 用户流程：** `<结果>`
- **[工具生成] Commit：** `<ID>`

## 10. 规则和记忆更新

- **[自动更新] 防止再次发生的规则：** `<路径或无需新增的理由>`
- **[自动更新] 模块地图、术语表和权威文档：** `<路径>`
- **[自动更新] 文档状态与索引：** `<结果>`

## 11. 面向项目负责人的反馈

- **[自动更新] 修复的功能问题：** `<不要写函数名>`
- **[自动更新] 用户以前会遇到什么：** `<现象>`
- **[自动更新] 现在会怎样：** `<正常行为>`
- **[自动更新] 用户怎样确认：** `<操作步骤>`
- **[自动更新] 最终影响：** `<用户收益>`
