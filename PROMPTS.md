# 可复用 AI 开发提示 V2

> 日常只需给出真实需求。文档、Git、测试和技术过程由 AI 处理；人重点审核终端用户结果、责任模块和架构方向。

## 1. 初始化现有项目

```text
把 AI 大型软件开发控制法 V2 应用到当前项目。

先盘点真实代码、目录、Git 状态、测试能力、终端用户流程和验证命令，不修改业务代码。

生成并填写：
- AGENTS.md；
- docs/INDEX.md；
- docs/system/PROJECT.md、ARCHITECTURE.md、MODULE_MAP.md、GLOSSARY.md、TESTING.md；
- docs/rules/ 常驻规则；
- docs/coordination/ACTIVE_TASKS.md；
- tasks、bugs、refactors、decisions、audits、reviews、handoffs、archive 目录；
- 当前工具需要的 CLAUDE.md 或 .cursor/rules 入口。

同时根据实际技术栈建立或确认：
- 一个统一的本地 verify 命令；
- 获得授权后的提交钩子；
- 存在远端仓库并获得授权时的最小 CI；
- 正式项目适用的必需检查和分支保护；
- 任务文档能够记录本地验证、CI 运行 ID/链接、合并和发布状态。

所有内容必须来自真实项目。明确标出测试、文档、密钥扫描和架构方面尚不存在的能力，不得虚构。

先用终端用户语言向我说明：这个项目提供什么功能、用户怎样使用、目前有哪些问题。技术盘点放在后面。

初始化生成后不要直接开始高风险功能开发。准备一份只读复核提示，让第二个 AI 对照真实代码审核 AGENTS.md、模块地图、术语表、测试现状、机器门禁和规则库。
```

## 2. 快速模式新需求

```text
按 AGENTS.md 快速模式处理。

需求：<粘贴项目负责人原话>

先检查 ACTIVE_TASKS，生成 `TYPE-YYYYMMDD-HHMMSS-AGENT-XXXX` 唯一 ID 并认领写入范围；再查 GLOSSARY.md、相关历史、规则和 MODULE_MAP.md，建立 TASK_LITE 文档。

先不要修改代码。请先用终端用户语言告诉我：
1. 你理解要做成什么功能；
2. 满足终端用户什么需求；
3. 用户怎样使用；
4. 页面或行为会怎样变化；
5. 哪些内容本次不做。

然后再列出责任模块、目标文件、禁止范围、Token 定位层级、自动化测试、机器门禁和 Git 检查点。记录“人审-用户”结论、审核人和时间。技术细节不要穿插在用户功能说明中。
```

## 3. 严格模式新需求

```text
按 AGENTS.md 严格模式处理。

需求：<粘贴项目负责人原话>

现在不要修改代码。先：
1. 检查 ACTIVE_TASKS，生成唯一任务 ID 并认领写入范围；
2. 查 GLOSSARY.md，用统一的终端用户术语复述需求；
3. 说明功能、使用方式、可见变化、用户收益和非目标；
4. 按 Token 定位阶梯查历史、规则、架构、模块和文件；
5. 指出责任模块、目标目录、目标文件和调用入口；
6. 说明禁止修改范围及禁止兜底、兼容、双写和重复实现；
7. 检查依赖、权限和密钥风险；
8. 记录 Git 基线、任务分支、远端权限和回退计划；
9. 设计同批交付的自动化测试、统一 verify、CI 和终端用户验收；
10. 创建完整 TASK 文档，等待我审核用户方向；
11. 准备由第二个 AI 对目标计划或 Commit 进行只读技术审核。
```

## 4. 批准后执行

```text
计划批准。按照任务文档执行，不得擅自扩大范围。

- 保护用户未提交修改；
- 功能代码与自动化测试同批完成；
- 实际情况与计划不符时暂停并更新计划；
- 不引入未经批准的依赖、权限、兜底、兼容或第二套实现；
- 运行终端用户流程和统一本地 verify；
- 按授权创建引用任务 ID 的 Git 检查点并推送到已确认的私有远端；
- 等待适用的远端 CI，并记录运行链接、Commit 和结果；
- 严格任务交给第二个 AI 对目标 Commit 只读复核；
- 更新权威文档、索引、术语、规则、任务认领和交接记录。

完成反馈先按终端用户视角说明功能、需求、使用方式、页面或行为变化、用户收益和验收方法，并分别报告本地验证、CI、合并和发布状态。除非我询问，不要在主反馈中堆代码、文件名和专业术语。
```

## 5. Bug 诊断

```text
按 AGENTS.md 和 BUG 流程处理。

问题：<粘贴项目负责人原话>

不要直接修代码。先：
1. 检查 ACTIVE_TASKS，生成唯一 Bug ID 并认领写入范围；
2. 用终端用户语言说明用户在做什么、看见什么、应该怎样以及受到什么影响；
3. 查 GLOSSARY.md、历史 Bug、规则和模块地图；
4. 用真实日志和步骤稳定复现；
5. 把复现固化为修复前会失败的自动化测试；
6. 沿调用链定位责任模块和根因；
7. 明确禁止吞错、默认值、无证据重试、兜底和重复实现；
8. 记录 Token 定位层级、Git 基线、CI 和回退计划；
9. 创建 BUG 文档并等待我审核用户结果，由第二个 AI 复核技术结论。

证据不足时明确说尚未定位，不得把猜测写成根因。
```

## 6. 审核 AI 计划

```text
重新审核当前计划，不要默认原计划正确，也不要修改代码。

先从终端用户角度检查：
- 功能是否说清楚；
- 是否满足原始需求；
- 用户怎样使用；
- 页面或行为变化是否符合预期；
- 是否遗漏非目标或边界。

再由审核 AI 只读检查：
- 标准术语、责任模块和目标文件；
- 单向依赖、业务边界和权威实现；
- 兜底、兼容、双写和重复实现风险；
- 自动化测试是否与功能同批交付；
- Git 基线、检查点和回退计划；
- 新依赖、许可证、权限和密钥风险；
- 文档是否会重复已有权威事实。

每个技术结论附一句给非程序员项目负责人的解释，说明它会怎样影响功能、稳定性、安全或后续维护。

输出通过项、问题项和修订方案。技术检查与用户功能说明分开呈现。
```

## 7. 架构审计和重构

```text
按重构模式审计 <模块或范围>。

先说明受影响的终端用户功能、当前用户问题以及重构后用户行为是否会变化。

再检查：
- 模块职责和单向依赖；
- 循环依赖、跨层调用和重复实现；
- 兜底、兼容和双写堆积；
- AI 是否因结构混乱读取大量无关代码；
- 文档、规则、测试和代码是否一致；
- Git 历史是否存在可回退基线。

先检查 ACTIVE_TASKS 并认领范围，创建架构审计文档，提出独立分支/工作区、分步验证、自动化测试、本地 verify、远端 CI、Git 检查点和第二 AI 只读复核计划。目标是删除混乱和缩短认知路径，不是增加包装层。
```

## 8. 沉淀常驻规则

```text
评估当前任务中的问题是否可能重复发生。

如果会重复，使用 RULE 模板起草规则，包含：触发条件、适用范围、必须动作、禁止动作、验证方法和不适用情形。先检查现有规则，能更新原规则就不要新建近义规则。

先让我审核规则，再加入 docs/rules/ 并更新规则索引和工具入口。
```

## 9. 多 AI 技术交叉审核

```text
你是当前任务的技术审核 AI。只读审核，不要修改作者工作区。

先阅读 AGENTS.md、规则、原始需求、任务文档、目标 Commit、自动化测试和机器验证证据。

审核：
- 用户功能是否符合原始需求；
- 责任模块、写入范围和架构边界；
- 测试是否真正覆盖用户行为；
- Git 基线、Commit、回退和远端 CI；
- 依赖、许可证、密钥和权限；
- 文档 SSOT、规则和任务认领是否正确。

使用 REVIEW 模板。每个技术结论都附一条用户语言证据摘要，让非程序员项目负责人能够理解风险。输出“通过 / 修改后复审 / 阻止”。不要直接修代码，修订交回作者 AI。
```

## 10. 初始化交叉审核

```text
只读审核当前项目初始化结果，不修改文件。

对照真实代码和实际命令检查：AGENTS.md、PROJECT、ARCHITECTURE、MODULE_MAP、GLOSSARY、TESTING、规则库、ACTIVE_TASKS、统一 verify、CI 和分支保护。

重点找出虚构能力、错误模块归属、过期术语、缺失用户流程、不能实际运行的验证命令和未被机器强制的规则。每个技术问题附用户语言影响。初始化审核未通过前，阻止高风险功能开发。
```

## 11. 新会话恢复

```text
先阅读 AGENTS.md、规则索引、当前任务和最新 HANDOFF。

先用终端用户语言复述：
1. 要交付什么功能；
2. 用户当前可以和不可以做什么；
3. 页面或行为当前状态；
4. 最终用户收益。

再说明下一步、责任模块、任务认领、Token 定位层级、Git 基线、本地验证、CI、合并/发布状态、禁止事项和最近审核。未经确认不要继续修改代码。
```

## 12. 完成反馈重写

```text
把当前技术完成报告重写成项目负责人和终端用户能理解的版本。

按以下顺序：
1. 功能名称；
2. 功能是什么；
3. 满足什么需求；
4. 用户怎样使用；
5. 页面、状态或流程发生什么变化；
6. 最终给用户带来什么；
7. 用户怎样确认已经可用；
8. 真实注意事项。

另外分别说明：本地验证是否通过、远端 CI 是否通过、是否已经合并、是否已经发布。不得把其中一个状态冒充另一个。

不要假设项目负责人是专业程序员。主反馈不要穿插代码、函数名、文件清单和专业术语。只有我明确要求时，才在最后增加技术附录。
```
